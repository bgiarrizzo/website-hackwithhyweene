<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ site.url }}/links/</id>
  <title>{{ site.short_url }} - Liens</title>
  <subtitle>{{ site.description }}</subtitle>
  <link rel="self" type="application/atom+xml">{{ site.url }}/links/feed.xml</link>
  <link>{{ site.url }}/links/</link>

  {% set last_link = links|last %}
  <updated>{{ last_link.publish_date }}</updated>

  <author>
    <name>{{ site.author }}</name>
    <uri>{{ site.url }}</uri>
  </author>
  
  {% for link in links %}
    <entry>
      <id>{{ site.url }}/links/{{ link.slug }}/</id>
      <title>{{ link.title }}</title>
      <link>{{ site.url }}/links/{{ link.slug }}/</link>
      <published>{{ link.publish_date }}</published>
      <updated>{{ link.update_date }}</updated>
      {% if link.summary %}
        <summary>
          {{ link.summary }}
        </summary>
      {% endif %}
      <content>
        {{ link.body }}
      </content>
    </entry>
  {% endfor %}
</feed>
