<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ site.url }}/blog/</id>
  <title>{{ site.short_url }}</title>
  <subtitle>{{ site.description }}</subtitle>
  <link rel="self" type="application/atom+xml">{{ site.url }}/blog/feed.xml</link>
  <link>{{ site.url }}/blog/</link>

  {% set last_post = posts|last %}
  <updated>{{ last_post.publish_date }}</updated>

  <author>
    <name>{{ site.author }}</name>
    <uri>{{ site.url }}</uri>
  </author>

  {% for post in posts %}
    <entry>
      <id>{{ site.url }}/blog/{{ post.slug }}/</id>
      <title type="html">{{ post.title }}</title>
      <link rel="alternate" type="text/html">{{ site.url }}/blog/{{ post.slug }}/</link>
      <published>{{ post.publish_date }}</published>
      <updated>{{ post.update_date }}</updated>
        
      {% if post.summary %}
        <summary>
          {{ post.summary }}
        </summary>
      {% endif %}
      <content type="html">
        {{ post.body }}
      </content>
    </entry>
  {% endfor %}
</feed>
